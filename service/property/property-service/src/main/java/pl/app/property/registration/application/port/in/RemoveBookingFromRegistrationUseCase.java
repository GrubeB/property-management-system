package pl.app.property.registration.application.port.in;

public interface RemoveBookingFromRegistrationUseCase {
    void removeBookingFromRegistration(RemoveBookingFromRegistrationCommand command);
}
